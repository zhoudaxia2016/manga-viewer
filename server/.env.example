# Copy to .env and fill in. Do not commit .env.

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_BASE=
DB_PATH=
OCR_SPACE_API_KEY=
# Set to 1 to log full ocr.space JSON responses (verbose; avoid in production).
OCR_DEBUG=

PASSWORD=
# GENERAL 含 GET /api/manga 列表；Vite 开发时 React Strict Mode 可能让首页列表连打 2 次。
RATE_LIMIT_GENERAL=100
RATE_LIMIT_IMPORTANT=20
